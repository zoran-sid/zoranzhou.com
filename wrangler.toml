# Cloudflare Pages configuration
# Build settings are configured in the Cloudflare dashboard

[build]
command = "npm run build"
publish = "dist"

[build.environment]
NODE_VERSION = "20"
name = "zoran-zhou-blog"
compatibility_date = "2026-06-29"

[assets]
directory = "./dist"